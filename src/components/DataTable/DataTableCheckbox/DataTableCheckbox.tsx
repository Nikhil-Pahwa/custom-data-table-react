import './DataTableCheckbox.scss';

export interface ITableCheckboxProperties {

}

const DataTableCheckbox: React.FC<ITableCheckboxProperties> = ({ }) => {
    return (
        <input type="checkbox" />
    );
}

export default DataTableCheckbox;
